---
"pi-freeflow": patch
---

Fresh installs now log full HTTP lifecycle debug output by default, so error reports can include complete request lifecycles; turn it off anytime with `/freeflow debug off`.
